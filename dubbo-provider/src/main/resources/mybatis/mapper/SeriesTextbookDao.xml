<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talkplatform.dubbo.provider.dao.SeriesTextbookDao">

	<select id="queryId" resultType="java.util.Map">
		SELECT id,name
		FROM series_textbook
		WHERE id = #{id}
	</select>

</mapper>